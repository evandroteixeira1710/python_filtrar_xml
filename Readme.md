### PAra iniciar o ambiente de desenvolvimento virtual do python python -m venv venvhtml, em seguida para iniciar .\venvhtml\Scripts\activate ###

### Arquivo que lê o html e filtra se tem cve diferentes de zero ###

### Para compilar o arquivo python em um .exe é necessário efetuar o pip install pyinstaller e executar o comando: pyinstaller --onefile .\xml.py ###